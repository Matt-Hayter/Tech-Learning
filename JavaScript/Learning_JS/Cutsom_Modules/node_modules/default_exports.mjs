// For use of custom modules when using node (not html/Vue)
export default class Fruit
{
    constructor(type) {
        this.type = type
    }
    getType() {
        console.log(this.type)
        return this.type
    }
}