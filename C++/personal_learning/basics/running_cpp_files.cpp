
#include <iostream>

int main() {
  // Compile with GNU (MinGNU on windows):
  // g++ reverse.cpp

  // Compile with custom binary name:
  // g++ reverse.cpp -o name

  // Run executable on Bash:
  // ./file.exe

  // Compile and run in Bash ("a" is default executable name):
  // g++ reverse.cpp && ./a.exe

  return 0;
}